package es.caib.sistra.persistence.delegate;

/**
 * Representa un delegate sin estado, i por tanto cacheable.
 */
public interface StatelessDelegate extends Delegate {
	
		// TODO En caso de cluster hay que reconsiderar el uso de cached homes
	
}
